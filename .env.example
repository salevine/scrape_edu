# Serper.dev API key (get free at https://serper.dev)
SERPER_API_KEY=

# Optional: Override output directory (default: ./output)
# OUTPUT_DIR=./output

# Optional: Override IPEDS data directory (default: ./data/ipeds)
# IPEDS_DIR=./data/ipeds
